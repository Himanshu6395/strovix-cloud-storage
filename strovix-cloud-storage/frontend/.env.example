# Local development — call the local backend directly
VITE_API_URL=http://localhost:5000/api

# Production builds use .env.production (VITE_API_URL=/api) and Vercel rewrites
# /api/* → https://harsh-production-c4a6.up.railway.app/api/*
