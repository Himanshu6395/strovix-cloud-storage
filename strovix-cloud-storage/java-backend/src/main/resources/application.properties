# ==========================================
# Storvix Application Configuration
# ==========================================
spring.application.name=storvix-java-backend
server.port=${PORT:5000}
server.forward-headers-strategy=framework

# ==========================================
# Frontend CORS
# ==========================================
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}

# ==========================================
# Jackson Configuration
# ==========================================
# Match Node.js output (camelCase, ignore nulls if needed)
spring.jackson.property-naming-strategy=LOWER_CAMEL_CASE
spring.jackson.default-property-inclusion=non_null

# ==========================================
# Supabase PostgreSQL
# ==========================================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# JWT
# ==========================================
jwt.secret=${JWT_SECRET:default-secret-key-that-must-be-at-least-256-bits-long!}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ==========================================
# OAuth2 Google
# ==========================================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:google-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:google-client-secret}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# ==========================================
# Native AWS S3
# ==========================================
aws.s3.region=${AWS_REGION:ap-south-1}
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.s3.access-key=${AWS_ACCESS_KEY_ID}
aws.s3.secret-key=${AWS_SECRET_ACCESS_KEY}


brevo.api-key=${BREVO_API_KEY:}
gemini.api-key=${GEMINI_API_KEY:}
